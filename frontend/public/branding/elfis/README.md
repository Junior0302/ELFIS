# ELFIS Home Hero visual

Place the optional hero artwork here:

- File: `home-hero-visual.webp`
- Served as: `/branding/elfis/home-hero-visual.webp`

If the file is missing, `HomeHeroVisual` falls back to the official Pilot Mark + soft halo (no broken image).
