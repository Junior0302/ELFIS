import AdminEquipePage from '../AdminEquipePage'

/** Membres, invitations, rôles — surface ELFIS Core (/platform/members). */
export default function PlatformMembersPage() {
  return <AdminEquipePage />
}
