import OrganisationPage from '../OrganisationPage'

/** Identité organisation — surface ELFIS Core (/platform/organization). */
export default function PlatformOrganizationPage() {
  return <OrganisationPage />
}
