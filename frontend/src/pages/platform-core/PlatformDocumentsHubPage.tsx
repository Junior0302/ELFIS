import DocumentsPage from '../DocumentsPage'

/** Vault global ELFIS — /platform/documents (surface=platform). */
export default function PlatformDocumentsHubPage() {
  return <DocumentsPage surface="platform" />
}
