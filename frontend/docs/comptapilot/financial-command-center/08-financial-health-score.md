# 08 — Financial Health Score

## Source

`overview.health` (`score`, `grade`, `state`, `components`, `message`).

## Affichage

- `state === 'setup'` → empty widget (message Engine)
- Score actif → valeur + grade + facteurs composants
- Recommandations Engine (slice UI, pas de reformulation métier)

## Disclaimer obligatoire

> Indicateur de pilotage — ne remplace pas un conseil comptable.

Affiché dans le widget Comprendre (et empty description setup).
