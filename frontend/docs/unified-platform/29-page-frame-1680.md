# 29 — ElfisPageFrame contrat 1680

| Règle | Valeur |
|-------|--------|
| width | 100% |
| max-width | **1680px** |
| margin | auto |
| padding-inline | 32 / 24 / 20 / 16 |
| padding-block | 24–40 |
| data | `data-elfis-page-frame="v1"` `data-page-frame-max="1680"` |

Interdit sous frame : `max-width < 1200` desktop large, `fit-content`, double container (viewport pad = 0).

Voir `ElfisPageFrame.tsx` + `unified-platform.css`.
