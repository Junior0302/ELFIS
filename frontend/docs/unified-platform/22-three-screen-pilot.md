# 22 — Three-screen pilot

| Écran | Changements |
|-------|-------------|
| Home | **ElfisPageFrame** + ElfisDashboardTemplate (header hero, main apps, aside rail 8/4) |
| FCC | **ElfisPageFrame** + ElfisDashboardTemplate (header + strip) ; KPI via PlatformGrid 12 ; plus de PlatformPageContainer |
| Sales | **ElfisPageFrame** via template (plus de colonne xl 1200) ; MetricCard + actions |

Données / APIs / calculs **préservés**. Largeur unique ~1600px — voir [25](./25-page-frame-layout-control.md).
