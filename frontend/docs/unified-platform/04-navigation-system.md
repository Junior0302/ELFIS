# 04 — NavigationSystem

Config `ElfisNavigationSection` / `ElfisNavigationItem` → `DomainNav` / `GlobalNavLinks` / `ContextualSubNav`.

Les Pilots fournissent la config, pas du JSX chrome ad hoc. Adapters runtime (ComptaProductNav, SalesProductNav) restent ; contrat unifié prêt.
