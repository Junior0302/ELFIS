# 30 — ElfisDashboardTemplate sections

Slots : Header, ContextStrip (`strip`), KPIGrid (`metrics`), PrimaryAnalysis, SecondaryAnalysis, Operations, RecentActivity (masqué si vide).

Legacy : `children` + `aside` 8/4 si pas de `primaryAnalysis`.

Alias grille : `ElfisDashboardGrid` (= PlatformGrid 12/8/4), gaps 24/20/16.
