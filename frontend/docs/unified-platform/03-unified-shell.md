# 03 — ElfisUnifiedShell

Consolide `PlatformShell` : GlobalTopbar + PilotSidebar + PilotContentLayout.

- UI.P1 collapse 240/56 préservé
- UI.P2 un hamburger
- `PilotWorkspace` = wrapper métier (pilotId, nav, title)
- Pas d’iframe / hard reload
