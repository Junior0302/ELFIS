# 12 — Dashboard template

`ElfisDashboardTemplate` : **ElfisPageFrame** (1680) + Header → ContextStrip → KPIGrid → PrimaryAnalysis → SecondaryAnalysis → Operations → RecentActivity.

Legacy : `children` + `aside` 8/4 si pas de `primaryAnalysis`.

Voir [25](./25-page-frame-layout-control.md), [29](./29-page-frame-1680.md), [30](./30-dashboard-sections.md).
