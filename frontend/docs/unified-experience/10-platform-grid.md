# 10 — PlatformGrid

Composants : `PlatformGrid` + `GridItem`

| Prop | Valeurs |
|------|---------|
| `columns` | 12 (défaut), 8, 4 |
| `gap` | token space foundation |
| `span` / `spanMd` / `spanLg` | 1–12 |

Complète `ds-grid` (1–4 / auto-fit) sans le supprimer.  
Responsive : ≤899px → grille 4 colonnes.
